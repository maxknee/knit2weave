<p id="notice"><%= notice %></p>
<%= render :partial => @item %>

<p>
	<strong>Price:</strong>
	<%= @item.price %>
</p>

<p>
	<strong>URL:</strong>
	<%= link_to @item.etsylink, "http://#{@item.etsylink}" %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
